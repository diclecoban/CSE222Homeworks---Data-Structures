
/**
 * A class containing the main method to run the social network menu application.
 */
public class Main {
    /**
     * The main method to start the social network menu application.
     *
     * @param args The command line arguments (not used).
     */
    public static void main(String[] args) {
        Menu menu = new Menu();
        menu.displayMenu();
    }
}
